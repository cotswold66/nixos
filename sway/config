exec dex --autostart -s ~/.config/autostart/

# Your preferred terminal emulator
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# output * bg $HOME/.local/share/backgrounds/p51XiSX-linux-background.jpg fill
#output * bg /usr/share/wallpapers/DarkestHour/contents/images/2560x1600.jpg fill

bindswitch --reload --locked lid:on output $laptop disable
bindswitch --reload --locked lid:off output $laptop enable



    # Drag floating windows by holding down Mod4 and left mouse button.
    # Resize them with right mouse button + Mod4.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier Mod4 normal



exec lxpolkit &
# exec_always /usr/lib/polkit-kde-authentication-agent-1
  
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

exec ~/.config/sway/homescreen.sh

#font pango:Source Code Pro 11
font Roboto 10

for_window [app_id="virt-manager"] floating enable
assign [app_id="virt-manager"] 3
assign [app_id="gnucash"] 6

for_window [class="zoom"] floating enable
assign [class="zoom"] 3

for_window [class="1Password"] floating enable

for_window [app_id="org.kde.dolphin"] floating enable

# gaps inner 5
default_border pixel 5

exec wl-paste -t text --watch clipman store --no-persist
# exec nm-applet --indicator
# exec mako

include /etc/sway/config.d/*
include ~/.config/sway/grimshot.sh

